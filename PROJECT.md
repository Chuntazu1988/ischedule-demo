# iSchedule — מערכת שיבוץ צוותי דיילים בטרמינל

## מה זה
אפליקציית Streamlit לשיבוץ אוטומטי של צוותי קרקע (דיילים, ראשי צוות, מתאמי תורים, שומרי TSA
ועוד) לטיסות יציאה בנתב"ג — טרמינל 3 וטרמינל 1. המערכת קולטת קובץ סידור עובדים יומי (Excel)
וקובץ טיסות, ובונה ממנו שיבוץ מלא: מי מבצע איזה תפקיד באיזו טיסה, מתי ההפסקות/רענונים, מתי
חוזרים לדלפקים או הביתה, ומעברים בין טרמינלים.

## איך מריצים
```
streamlit run streamlit_app.py --server.port 8501
```
יש גם `streamlit_app_gantt.py` — תצוגת גאנט חלופית, רץ בנפרד על פורט 8502.

## מבנה הפרויקט

```
streamlit_app.py         האפליקציה הראשית — UI, טאב "זרימת עבודה", לוגיקת תצוגה
streamlit_app_gantt.py   תצוגת גאנט חלופית
app/
  scheduler.py           מנוע השיבוץ עצמו (build_schedule ועוד) — הלוגיקה העסקית המרכזית
  display.py             בניית טבלאות פלט, תוויות "המשך אזורי", הפסקות/רענונים, חזרות
  styles.py               CSS/עיצוב
data/
  data_loader.py         פענוח קבצי האקסל (סידור יומי, עובדים), זיהוי משמרות/הערות/מעברי טרמינל
utils/
  helpers.py              פונקציות עזר: סיווג משמרות, חלונות הפסקה מועדפים, פורמט זמן וכו'
  constants.py             קבועים (עמודות, תפקידים)
```

היקף קוד גס: כ-14,000 שורות (`streamlit_app.py` הכי גדול, ~5,700 שורות).

## זרימת הנתונים
1. **קלט**: קובץ "סידור יומי" (Excel, גיליונות לפי סוג תפקיד: של"ן, דלפקי ש"ש, סיירת וכו') +
   קובץ עובדים (`employees_clean.xlsx`) + קובץ טיסות. אופציונלי: קובץ סידור נפרד לטרמינל 1 (TR1).
2. **`data_loader.build_shift_map_from_excel`**: סורק את הסידור היומי ומחלץ לכל עובד את שעות
   המשמרת, הערות (מחלה, מעבר טרמינל, "טרייני -" קידומת, "טרייני ר"צ" סיומת), חסימות, ומשמרות
   נוספות (עובד עם שתי משמרות נפרדות באותו יום).
3. **`data_loader.apply_shift_map_to_employees`**: ממזג את הסידור היומי לתוך טבלת העובדים —
   כולל טיפול ב"עמוד 1" (זנב משמרת לילה שהתחילה אתמול) מול "עמוד 2" (משמרות של היום), התאמת
   שמות (כולל fallback מבוסס חפיפת מילים לשמות לא-מדויקים), ופיצול משמרת בעת מעבר בין טרמינלים.
4. **`scheduler.build_schedule`**: מנוע השיבוץ — מקצה עובדים לטיסות לפי כשירות, זמינות, איזון
   עומס, וכללי עדיפות (ר"צ/טרייני ר"צ/דיילים/שומרי TSA/מתאמי תורים), כולל טיפול במילוי פערים
   (gap-fill) ורצף עבודה לראשי צוות (`optimize_tl_continuity`).
5. **`display.build_next_task_labels` / `build_output_table`**: מוסיפים לכל שיבוץ תוויות תצוגה —
   מה המשימה הבאה, מתי ההפסקה/רענון (`_break_slot` cascade לפי חלון מועדף לכל סוג משמרת), מתי
   חזרה לדלפקים/הביתה, הערות מעבר טרמינל.
6. **`streamlit_app.py`**: מציג את התוצאה במספר טאבים — טבלת שיבוץ לפי טיסה, "זרימת עבודה" (תצוגה
   לפי עובד, כרונולוגית, עם צ'יפים לכל משימה/הפסקה/חזרה), עומס עבודה, מרכז בקרה, ניהול
   הפסקות/משובצים.

## מושגים מרכזיים בדומיין
- **עמוד 1 מול עמוד 2**: עמוד 1 = זנב משמרת לילה שהחלה אתמול (כבר מתרחשת הבוקר); עמוד 2 = הסידור
  הטרי של היום.
- **טרייני ר"צ** (ראש צוות בהכשרה, מסומן בסיומת בסידור) — שונה לגמרי מ**דייל בטרייני** (דייל חדש
  שמוצמד לדייל ותיק, מזוהה מקידומת "טרייני - שם" בסידור).
- **ילד עובדים / פורש**: קבוצת עובדים מוגבלת (יכולים למלא רק תפקידי שומר TSA ודייל).
- **מעבר בין טרמינלים**: עובד שמתחיל משמרת בטרמינל אחד ועובר לשני באמצע — כולל הפסקה +
  ~15 דקות נסיעה, ומסומן הן בשעות המשמרת המוצגות והן כצ'יפ ייעודי.
- **הפסקה מול רענון**: משמרת ≥10 שעות מקבלת גם הפסקה (45 דק') וגם רענון (20 דק'); 6-10 שעות —
  הפסקה בלבד; פחות מ-6 — רענון בלבד.
- **חלון הפסקה מועדף** (`preferred_break_window_by_shift`): לכל סוג משמרת (לילה, בוקר מוקדם,
  יום, צהריים, ערב) יש טווח שעות מומלץ להפסקה, לפי דפוס העומס הטיפוסי.

## מוסכמות שהתבררו חשובות תוך כדי עבודה
- **תמיד לאמת מול נתונים אמיתיים** — להעתיק קבצי אקסל אמיתיים לתיקיית scratch ולהריץ את
  הפונקציות בפועל, לא לנחש מקריאת קוד/צילומי מסך בלבד.
- **מטמון קבצים**: `streamlit_app.py` שומר טביעת אצבע (MD5) של קבצי הקלט שהועלו; תיקון בלוגיקת
  `data_loader.py` (פענוח הסידור) לא ייכנס לתוקף עד **הפעלה מחדש של שרת ה-Streamlit** (לא מספיק
  ללחוץ "בנה שיבוץ" מחדש, ואפילו לא להעלות מחדש את אותו קובץ — הטביעה מבוססת תוכן).
- שינויים בלוגיקת תצוגה (`app/display.py`, `streamlit_app.py`) כן נכנסים לתוקף בכל בנייה מחדש
  של השיבוץ, בלי צורך בהפעלה מחדש.

## קבצים שלא לגעת בהם בלי זהירות
- ה-cascade של מיקום הפסקות ב-`app/display.py` (`_break_slot`) הוא "רפרנס נעול" מול דוגמאות
  שאושרו כנכונות (TL#7, TL#10, TL#8) — שינויים שם דורשים אימות קפדני.
